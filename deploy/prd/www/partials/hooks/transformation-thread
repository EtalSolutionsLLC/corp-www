#!/usr/bin/env bash
set -euo pipefail

SITE_DIR="${SITE_DIR:-$(pwd -P)}"
REPO_ROOT="${REPO_ROOT:-$(cd "$SITE_DIR/.." && pwd -P)}"

cd "$REPO_ROOT"

bin/rotate-transformation-thread --force >/dev/null

cat "$SITE_DIR/partials/transformation-thread.html"
